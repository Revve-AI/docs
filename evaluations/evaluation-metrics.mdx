---

title: "Evaluation Metrics"
description: "The team-wide library of reusable scoring criteria — deterministic checks and LLM-judged rubrics — shared by Evaluators and Scorecards."
---

**Metrics** (**Evaluations → Metrics** in the left dashboard nav) is a standalone library where a team defines reusable scoring criteria once and attaches them wherever calls or conversations get graded. Metrics belong to your team, not to a single agent or Scorecard — a metric you build here is the same metric you can add to an [Evaluator](/evaluations/creating-an-evaluator) or a [Scorecard](/evaluations/scorecards), which a Voice Agent then attaches for live-call grading. This page covers both the library UI (create, edit, delete) and the scoring mechanics (Deterministic vs. LLM Judge, the seven deterministic checks, Binary vs. Scale 1-5, speaker scope, score rubrics) — that mechanics doesn't change based on where the metric ends up attached.

![The Metrics library page, showing the metrics table with Name, Type, Description, and Created columns, and the New Metric button](/screenshots/evaluations-metrics-list.png)

<Note>
One team-wide library, two direct consumers (and a third, indirectly). A metric created here can be attached to:

- An **Evaluator**, in the top-level Evaluations product — see [Creating an Evaluator](/evaluations/creating-an-evaluator).
- A **Scorecard** — see [Scorecards](/evaluations/scorecards). A Voice Agent doesn't attach metrics directly; it attaches Scorecards, which attach metrics — see [Voice Agent Evaluation](/voice-agents/voice-agent-evaluation).

Editing a metric here changes it everywhere it's attached — there's no per-consumer override of a metric's own definition, only of its weight and threshold within each policy.
</Note>

## Creating a metric

Click **New Metric** (top right, also shown in the empty state) to open a side panel titled **New Metric**. Editing an existing metric — click its row — opens the same panel titled **Edit Metric**.

| Field | Notes |
|-------|-------|
| **Name** | Required. Placeholder example: "e.g. Verified Identity". Keep it specific enough to recognize in a weighted-metrics table on a Scorecard or Evaluator, where you won't see the full description. |
| **Description** | Optional. Placeholder: "What this metric checks...". Worth filling in — it's the only context a teammate attaching your metric to their own Scorecard sees before they add it. |
| **Type** | Required. **Deterministic** or **LLM Judge** — see the two field groups below. |
| **Informational only** | Checkbox, independent of Type. When checked, the metric is still judged and shown on every scored call, but excluded from the parent Scorecard or Evaluator's overall pass/fail score — useful for something worth watching without letting it sink a result on its own. |

**When Type is Deterministic:**

| Field | Notes |
|-------|-------|
| **Check Type** | Defaults to **Tool Called**. Seven checks are available: **Tool Called** (tool appears in the call's tool call logs), **Tool Not Called** (tool does NOT appear), **Tool Called With** (tool was called with matching parameters), **Contains Text** (an agent message contains a substring), **Not Contains Text** (the agent never says a given substring), **Regex Match** (an agent message matches a regex pattern), **Turn Count** (the conversation completed within N turns). These are pass/fail because they're facts, not opinions — no LLM call is needed, so they're also cheap and instant. |

**When Type is LLM Judge:**

| Field | Notes |
|-------|-------|
| **Scoring** | Radio: **Binary (pass/fail)** ("Judge returns true or false. Default.") or **Scale 1-5** ("Judge assigns a score (1-5) using your rubric. Default pass threshold is 4."). Binary is enough for a yes/no policy check; reach for Scale 1-5 when you actually want a numeric trend to watch over time — it costs you five rubric fields up front, but a metric that's always going to be pass/fail doesn't need it. |
| **Judge Prompt** | The grading instruction sent to the LLM alongside the transcript. Placeholder example in the app: "Did the agent verify the customer's identity before providing account information?" Write it as a specific yes/no or gradable question, not a vague theme like "was the agent helpful". |
| **Who is being evaluated?** | Radio: **AI Agent** ("Score the agent's utterances. The caller's lines stay as context.") or **Caller** ("Score the caller's utterances. The agent's lines stay as context."). |
| **Score rubric — what merits each score for this metric?** | Only shown when Scoring is Scale 1-5. Five fields, **Score 1** through **Score 5**, each with placeholder "What in the conversation would warrant this score?". Fill in every level, not just the ends — a judge with no definition for "3" will assign one anyway, just inconsistently. |

Submit with the button at the bottom of the panel: **Create** for a new metric, **Save Changes** when editing.

<Tip>
Use the **Type** filter above the table (**All types** / **Deterministic** / **LLM Judge**) to check for near-duplicates before creating a new metric — it's easy to end up with two similarly-named LLM Judge metrics asking almost the same question, which just doubles judge cost without adding signal.
</Tip>

## Where metrics get used

| Consumer | What it's for | Docs |
|----------|----------------|------|
| **Scorecards** | A reusable, single-speaker (Agent or Caller) weighted policy — attach one to a Voice Agent for automatic live-call grading, or apply it to a batch of uploaded recordings. | [Scorecards](/evaluations/scorecards) |
| **Evaluators** | Pre-ship regression testing — grade a simulated or scripted test conversation before you publish an agent change. | [Creating an Evaluator](/evaluations/creating-an-evaluator) |

## Deleting a metric

The row-level delete action opens an **AlertDialog** titled **Delete metric**:

> This metric will be removed from all evaluators that use it. This action cannot be undone.

Read that literally and it undersells the blast radius: the confirmation copy only mentions evaluators, but the same metric may also be attached to a Scorecard — including one attached to a live Voice Agent. Deletion isn't scoped to one consumer — if you're not certain a metric is unused, check its usage on Scorecards as well as Evaluators before deleting it. There's no undo.

## What's Next

- [Scorecards](/evaluations/scorecards) — build a weighted policy from these metrics and attach it to a Voice Agent or a batch of recordings.
- [Voice Agent Evaluation](/voice-agents/voice-agent-evaluation) — attach a Scorecard to a Voice Agent for automatic live-call grading.
- [Creating an Evaluator](/evaluations/creating-an-evaluator) — build a pre-ship test case and attach metrics from this library to it.
